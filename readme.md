# Machine Learning 

# Python 
Python is high level coding langauge and it is interpreter program that reads and execute code instruction by instruction(runs code one by one.)   

use: 
```print("1")```  
```print("2")```  
```print("3")```  
now the output will be print  
1  
2  
3  
  
# How Python internally works 
Python code(.py)-> bytecode->interpreter->output

# Supervised Machine Learning and associated task(Regression,classification)